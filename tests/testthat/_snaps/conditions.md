# scans_abort() formats cli bullets

    Code
      scans_abort(c("Can't scan this trajectory.", x = "The trajectory has no events.",
        i = "Supply at least one message or tool event."))
    Condition
      Error:
      ! Can't scan this trajectory.
      x The trajectory has no events.
      i Supply at least one message or tool event.
