% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vitals-adapter.R
\name{as_trajectory_vitals}
\alias{as_trajectory_vitals}
\title{Convert vitals evaluation samples}
\usage{
as_trajectory_vitals(
  x,
  run_id = NULL,
  task_id = NULL,
  source_uri = NULL,
  include_system_prompt = TRUE
)
}
\arguments{
\item{x}{A vitals \code{Task}, or a data frame from \code{Task$get_samples()} or
\code{vitals::vitals_log_read()}. Sample data must contain unique \verb{(id, epoch)}
pairs and an ellmer \code{Chat} in \code{solver_chat} for every row. When \code{epoch} is
absent, it defaults to \code{1L}.}

\item{run_id, task_id}{Optional correlation identities applied to every
trajectory.}

\item{source_uri}{An optional source locator, such as the path to a vitals
eval log. Credentials, query parameters, and fragments are removed.}

\item{include_system_prompt}{Whether solver system prompts should be
included as system turns.}
}
\value{
A \link{TrajectoryBundle}.
}
\description{
\code{as_trajectory_vitals()} snapshots completed solver trajectories and their
evaluation outcomes from \href{https://vitals.tidyverse.org/}{vitals}. It accepts
a vitals \code{Task} or the sample data frame returned by \code{Task$get_samples()}.
It also accepts the data frame returned by the development
\code{vitals::vitals_log_read()} interface.
}
\details{
Each \verb{(id, epoch)} sample becomes one trajectory. Its \code{solver_chat} is
converted through the ellmer adapter, and a non-missing \code{score} becomes one
evaluation row. Sample input, result, solver metadata, and custom columns are
retained as sanitized trajectory metadata. Scorer metadata is retained on
the evaluation. A \code{scorer_chat}, when present, is recorded as an explicit
adapter loss because schema version 1 represents the evaluated solver path,
not the evaluator's separate model trajectory.

The adapter only uses the public \code{Task$get_samples()} method. It does not
retain the live task, chats, providers, tools, callbacks, or credentials.
}
\examples{
if (
  rlang::is_installed("ellmer", version = "0.4.2") &&
    rlang::is_installed("vitals", version = "0.3.0")
) {
  task <- vitals::Task$new(
    data.frame(input = "Say hello", target = "Hi"),
    solver = function(inputs) {
      chat <- ellmer::chat_openai(
        model = "gpt-4o-mini",
        credentials = function() "not-used"
      )
      chat$set_turns(list(
        ellmer::UserTurn(list(ellmer::ContentText(inputs[[1L]]))),
        ellmer::AssistantTurn(list(ellmer::ContentText("Hi")))
      ))
      list(result = "Hi", solver_chat = list(chat))
    },
    scorer = function(samples) list(score = "C"),
    dir = tempdir()
  )
  task$solve()
  task$score()

  bundle <- as_trajectory_vitals(task)
  trajectory_evaluations(bundle)
}
}
