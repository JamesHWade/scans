% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure-trajectories.R
\name{measure_trajectories}
\alias{measure_trajectories}
\title{Measure recorded trajectory resources}
\usage{
measure_trajectories(x)
}
\arguments{
\item{x}{A \link{TrajectoryBundle}.}
}
\value{
A tibble with one row per trajectory and measure:
\itemize{
\item \code{trajectory_id}, \code{parent_trajectory_id}: attribution; children are not
pooled with parents or subtracted from inclusive parent measurements.
\item \code{measure}, \code{value}, \code{unit}, \code{aggregation}: the quantity and its operation.
Durations are seconds (\code{s}); usage is \code{tokens}.
\item \code{n_recorded}, \code{n_total}: eligible records with a valid value and eligible
records in the loaded snapshot, respectively. Unknown counts are \code{NA}.
\item \code{coverage}: \code{complete}, \code{partial}, \code{unavailable}, or \code{unknown} (an older
conversation total without a denominator). Complete recording does not
establish complete capture of an application's work.
\item \code{source}, \code{source_unit}, \code{adapter}, \code{contract_version}: interpretation
provenance. Contract version 1 describes this measurement interface.
\item \code{includes_cached_input}: \code{TRUE} for input totals known to include cache
reads, otherwise \code{NA} (unknown or not an input-token measure).
\item \code{currency}, \code{price_basis}: missing until supported cost semantics exist.
\item \code{limitation}: the boundary on interpreting the value.
}
}
\description{
\code{measure_trajectories()} reports resource values with their recording
denominators and source semantics. It prefers captured OpenTelemetry calls
over reconstructed turns. \code{\link[=summarize_trajectories]{summarize_trajectories()}} retains its original
turn-based sums, including raw source costs.
}
\section{Measures}{

\code{elapsed} is time between trajectory bounds. New OTel captures require
valid bounds for every captured span; incomplete bounds are unavailable.
\code{input_tokens},
\code{output_tokens}, and \code{cached_input_tokens} sum known captured call values,
otherwise canonical assistant turns and other turns explicitly carrying
usage. For ellmer turns, including those projected by Deputy, dsprrr,
Commons, and vitals, input totals combine uncached and cached input per
turn. Both components must be recorded for a turn to contribute a known
input total; missing cache usage is not zero. Other canonical sources retain
their input counts with unknown cache inclusion. Do not add cached usage
again when \code{includes_cached_input} is \code{TRUE}.
\code{turn_duration} sums known canonical turn durations without asserting one
turn equals one model call.

\code{model_duration} sums captured OTel chat durations. \code{tool_duration} sums
OTel execute-tool durations, otherwise canonical tool-call durations.
Both include nested activity. \code{recorded_work} unions captured chat and tool
intervals, counting overlap once. \code{unattributed_elapsed} is elapsed minus
this union only when all captured intervals are valid. It does not prove
idle time. Do not sum per-trajectory intervals to claim application wall time.

\code{first_chunk_latency} is the median recorded OTel time to first chunk;
chunks may contain no token. \code{first_token_latency} and \code{cost} are unavailable
because supported sources do not establish these semantics. Raw cost sums
remain accessible through \code{\link[=summarize_trajectories]{summarize_trajectories()}}. Missing values never
become zero; partial sums describe only recorded components.
}

\examples{
bundle <- TrajectoryBundle(
  data.frame(trajectory_id = "example", source_type = "manual"),
  data.frame(), data.frame()
)
measure_trajectories(bundle)
}
