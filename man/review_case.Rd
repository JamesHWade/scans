% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review-case.R
\name{review_case}
\alias{review_case}
\alias{review_decision}
\alias{review_status}
\title{Review exact trajectory evidence}
\usage{
review_case(
  x,
  trajectory_id,
  finding_id = NULL,
  scan = NULL,
  event_ids = character(),
  turn_ids = character()
)

review_decision(
  x,
  judgment,
  reviewer,
  rationale,
  expected_behavior = NULL,
  supersedes = character()
)

review_status(x)
}
\arguments{
\item{x}{An \code{\link[=investigation_snapshot]{investigation_snapshot()}} for \code{review_case()}, or a review case
for \code{review_decision()} and \code{review_status()}.}

\item{trajectory_id}{One trajectory in the investigation.}

\item{finding_id}{Optional finding in that trajectory. Leave \code{NULL} to
review an unflagged trajectory or record a missed finding.}

\item{scan}{Optional scanner name from the saved analysis. Required to
record a scanner target for an unflagged trajectory. Inferred from a finding.}

\item{event_ids, turn_ids}{Optional evidence references within the trajectory.}

\item{judgment}{One of \code{"confirmed"}, \code{"rejected"}, \code{"uncertain"},
\code{"missed"}, or \code{"insufficient_capture"}.}

\item{reviewer}{One non-empty reviewer name.}

\item{rationale}{One non-empty explanation about the reviewed evidence.}

\item{expected_behavior}{Optional description of the intended future behavior.}

\item{supersedes}{Decision identifiers to adjudicate. Only currently active
decisions can be superseded. Defaults to none, creating an independent review.}
}
\value{
\code{review_case()} and \code{review_decision()} return a \code{scans_review_case}
value with selected evidence, provenance, and append-only decision history.
\code{review_status()} returns a list with \code{status} and active \code{decision_ids}.
}
\description{
\code{review_case()} freezes one trajectory from an investigation for human
review. It retains the original snapshot and revision identifiers and a
selected investigation containing only that trajectory and relevant losses.
\code{review_decision()} appends a decision without changing earlier decisions.
These functions never call a model or select content for an example set.
}
\details{
A scanner judgment describes whether a finding is valid. An application
expectation describes what a future execution should do. They are separate:
the rationale for an old output is not a score for a new prediction.

Case identifiers exclude the evidence copy's creation time, so reviewing
the same evidence selection again addresses the same case.
Decisions have content identifiers. Independent decisions remain active
until an adjudicator explicitly supersedes them. Multiple active decisions
are disputed, even if they agree. Uncertain and insufficient-capture
decisions cannot become evaluation targets. Reviewer names are supplied
labels, not authenticated identities. Hashes detect edits, not authorship.
}
