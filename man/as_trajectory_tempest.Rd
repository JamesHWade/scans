% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tempest-adapter.R
\name{as_trajectory_tempest}
\alias{as_trajectory_tempest}
\title{Convert completed Tempest trajectory reviews}
\usage{
as_trajectory_tempest(
  x,
  trajectory_id = NULL,
  source_uri = NULL,
  metadata = list()
)
}
\arguments{
\item{x}{A completed value returned by
\code{tempest::tempest_trajectory_review()}.}

\item{trajectory_id}{A bundle-unique trajectory ID. When \code{NULL}, a stable ID
is derived from the Tempest research run ID.}

\item{source_uri}{An optional source locator. Credentials, query parameters,
and fragments are removed.}

\item{metadata}{A uniquely named list of safe application metadata.
Sensitive fields are redacted and recorded as losses.}
}
\value{
A \link{TrajectoryBundle}.
}
\description{
\code{as_trajectory_tempest()} consumes the authoritative closed projection from
\code{tempest::tempest_trajectory_review_data()} and converts it into a canonical
\link{TrajectoryBundle}. It accepts the already-created review and never reaches
back into a \code{TempestResult}, session, workspace, provider, or other live
object.
}
\details{
One review becomes one product trajectory. Authoritative stage order is
preserved before the review's canonical-set lanes. Product stages, Deputy
agent runs, programs, knowledge, evidence identities, join proofs, and
findings remain distinguishable through namespaced events and structured
values. In particular, \code{authority_validated}, \code{exact_identity}, and
\code{correlation_only} proof kinds are retained without reinterpretation.

The Tempest review deliberately excludes prompts, responses, source
content, local paths, credentials, and live runtime objects. Those omissions
are recorded as adapter losses rather than reconstructed from private state.
}
\examples{
\dontrun{
review <- tempest::tempest_trajectory_review(result)
bundle <- as_trajectory_tempest(review)

filter_trajectory_events(bundle, event_type = "tempest:join")
}
}
