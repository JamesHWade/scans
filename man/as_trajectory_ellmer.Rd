% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ellmer-adapter.R
\name{as_trajectory_ellmer}
\alias{as_trajectory_ellmer}
\title{Convert completed ellmer conversations}
\usage{
as_trajectory_ellmer(
  x,
  trajectory_id = NULL,
  run_id = NULL,
  source_id = NULL,
  source_uri = NULL,
  task_id = NULL,
  sample_id = NULL,
  epoch = NULL,
  agent = NULL,
  model = NULL,
  metadata = list(),
  include_system_prompt = TRUE
)
}
\arguments{
\item{x}{A completed ellmer \code{Chat}, a list of ellmer turns, or one ellmer
turn.}

\item{trajectory_id}{A bundle-unique trajectory ID. When \code{NULL}, the adapter
uses the deterministic snapshot-local ID \code{"trajectory-000001"}.}

\item{run_id, source_id, task_id, sample_id}{Optional correlation identities.}

\item{source_uri}{An optional source locator. Credentials, query parameters,
and fragments are removed.}

\item{epoch}{An optional positive evaluation epoch.}

\item{agent}{An optional agent label.}

\item{model}{An optional model label. For a \code{Chat}, the public
\code{get_model()} value is used by default.}

\item{metadata}{A named list of safe application metadata. Sensitive keys
are redacted and recorded in the bundle's losses table.}

\item{include_system_prompt}{Whether a \code{Chat} system prompt should be
included as a system turn.}
}
\value{
A \link{TrajectoryBundle}.
}
\description{
\code{as_trajectory_ellmer()} snapshots a completed ellmer \code{Chat}, a list of
exported ellmer turn objects, or one ellmer turn into a canonical
\link{TrajectoryBundle}. It never sends a prompt or retains the live chat,
provider, tools, callbacks, or credentials.
}
\details{
\code{as_trajectory()} dispatches to this adapter for ellmer chats, turns, and
lists of turns when ellmer is installed.

Text, thinking, tool requests, tool results, remote images, inline images,
and PDFs receive canonical event representations. Tool results retain the
request ID and point to the matching request event when it is present.
Provider response JSON is excluded by default, inline binary content is
externalized, sensitive named fields are redacted, and unknown content
classes produce a namespaced custom event plus an explicit loss row.

Ellmer does not provide stable IDs for chats, turns, or content blocks. The
adapter therefore generates fixed-width IDs from source order. Re-converting
an unchanged snapshot produces the same IDs, while inserting or reordering
turns or content may change them.
}
\examples{
if (rlang::is_installed("ellmer", version = "0.4.2")) {
  turns <- list(
    ellmer::UserTurn(list(ellmer::ContentText("Hello"))),
    ellmer::AssistantTurn(list(ellmer::ContentText("Hi")))
  )

  bundle <- as_trajectory_ellmer(turns)
  trajectory_events(bundle)
}
}
