% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scans-tools.R
\name{scans_tools}
\alias{scans_tools}
\title{Create tools for exploring retained trajectories}
\usage{
scans_tools(x, trajectory_ids = NULL, max_rows = 100L, max_chars = 4000L)
}
\arguments{
\item{x}{A \link{TrajectoryBundle} or saved \code{\link[=investigation_snapshot]{investigation_snapshot()}}.}

\item{trajectory_ids}{Permitted trajectory identifiers. \code{NULL} permits all
retained trajectories; \code{character()} permits none. Model arguments can
narrow this scope but cannot broaden it.}

\item{max_rows}{Maximum rows returned by each query, from 1 to 1000.}

\item{max_chars}{Maximum characters per field, from 100 to 16000. Larger
fields are returned as explicit slices. Repeat the same paginated query
with \code{content_offset} to retrieve subsequent slices.}
}
\value{
A named list of ellmer tools: \code{scans_list_trajectories},
\code{scans_summarize}, \code{scans_read_trajectory}, \code{scans_find_events},
\code{scans_get_findings}, and \code{scans_measure}.
}
\description{
\code{scans_tools()} creates six read-only ellmer tools backed by deterministic
scans queries. The tools inspect a fixed copy of the supplied evidence; they
cannot fetch sources, replay recorded tools, execute R, or write reviews.
Register the returned tools with an ellmer chat using \code{chat$set_tools()}.
}
\details{
Tools return row-oriented JSON with data, snapshot and analysis identities,
evidence references, coverage limitations, and explicit pagination. Counts
and descriptive statistics are computed over the requested population in R,
not over the returned page. Missing values remain JSON \code{null}.

A saved investigation retains its saved diagnostics and scanner settings.
To recompute them, explicitly create a new \code{\link[=investigation_snapshot]{investigation_snapshot()}} from
its bundle and supply that separate investigation.
}
\examples{
if (rlang::is_installed("ellmer", version = "0.5.0")) {
  bundle <- TrajectoryBundle(
    data.frame(trajectory_id = "run-1", source_type = "manual"),
    data.frame(), data.frame()
  )
  tools <- scans_tools(bundle)
}
}
