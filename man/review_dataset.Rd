% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review-examples.R
\name{review_dataset}
\alias{review_dataset}
\title{Build explicitly mapped evaluation or optimization data}
\usage{
review_dataset(x, format, split)
}
\arguments{
\item{x}{An application example set from \code{\link[=review_examples]{review_examples()}}.}

\item{format}{Either \code{"dsprrr"} or \code{"vitals"}.}

\item{split}{Exactly one of \code{"development"} or \code{"test"}. Required so a
training call cannot silently include held-out cases.}
}
\value{
A tibble of explicitly mapped cases with provenance attached.
}
\description{
\code{review_dataset()} emits application inputs and targets for a consumer. It
never infers a runnable input from captured messages, calls a model, or
turns an old reviewer rationale into a new prediction score. Use a scorer
that evaluates each fresh output against its target and expected behavior.
}
\details{
The \code{"dsprrr"} format contains the named input and target columns required
by a module signature. The \code{"vitals"} format contains \code{id}, \code{input}, \code{target},
and \code{metadata} list columns; use a solver/scorer that accepts the explicit
mappings. Provenance, expected behavior, groups, and version are available
in the \code{scans_provenance} attribute of either format. Do not include that
attribute or old reviewer rationale as optimization targets by accident.
}
