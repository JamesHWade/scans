% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan-primitives.R
\name{trajectory_scan_primitives}
\alias{trajectory_scan_primitives}
\alias{filter_trajectory_events}
\alias{summarize_trajectories}
\alias{scan_trajectories}
\title{Filter, summarize, and scan trajectory bundles}
\usage{
filter_trajectory_events(
  x,
  trajectory_id = NULL,
  role = NULL,
  event_type = NULL,
  content_type = NULL,
  tool = NULL,
  status = NULL,
  metadata = NULL
)

summarize_trajectories(x)

scan_trajectories(
  x,
  scan_id = "scan-000001",
  repeat_threshold = 2L,
  loop_threshold = 3L
)
}
\arguments{
\item{x}{A \link{TrajectoryBundle}.}

\item{trajectory_id, role, event_type, content_type, tool, status}{Optional
character vectors. When supplied, an event must match one of the values
in every supplied filter.}

\item{metadata}{An optional named list of top-level event metadata values to
match exactly.}

\item{scan_id}{A non-empty identifier for this diagnostic run.}

\item{repeat_threshold}{The minimum number of calls with the same tool name
and arguments that produces a \code{repeated_tool_call} finding.}

\item{loop_threshold}{The minimum consecutive calls with the same tool name
and arguments that produces a \code{suspicious_tool_loop} finding. Tool results
between calls do not break a call sequence.}
}
\value{
\itemize{
\item \code{filter_trajectory_events()} returns a tibble of selected events.
\item \code{summarize_trajectories()} returns one tibble row per trajectory.
\item \code{scan_trajectories()} returns a tibble with one row per finding and the
event identifiers supporting that finding.
}
}
\description{
These functions provide the first composable post-run diagnostics for a
\link{TrajectoryBundle}. \code{filter_trajectory_events()} selects canonical events,
\code{summarize_trajectories()} returns one summary row per trajectory, and
\code{scan_trajectories()} emits deterministic findings for common tool and
error patterns.
}
\details{
\code{filter_trajectory_events()} joins each event to its containing turn for
role-based filtering and returns \code{role}, \code{turn_index}, and \code{round_index}
alongside the canonical event columns. A named \code{metadata} list matches
top-level event metadata entries using exact R equality.

\code{scan_trajectories()} detects unresolved tool calls, unmatched tool results,
repeated tool calls, suspicious consecutive tool-call loops, failed events,
and causal error chains. It analyzes a completed snapshot and never calls a
model or tool. Finding identifiers are deterministic for an unchanged
bundle, argument set, and \code{scan_id}.
}
\examples{
bundle <- TrajectoryBundle(
  data.frame(
    trajectory_id = "trajectory-1",
    source_type = "manual"
  ),
  data.frame(
    trajectory_id = "trajectory-1",
    turn_id = "turn-1",
    turn_index = 1L,
    role = "assistant"
  ),
  data.frame(
    trajectory_id = "trajectory-1",
    event_id = "event-1",
    event_index = 1L,
    turn_id = "turn-1",
    event_type = "content",
    content_type = "text",
    text = "Done"
  )
)

filter_trajectory_events(bundle, role = "assistant")
summarize_trajectories(bundle)
scan_trajectories(bundle)

}
