% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan-trajectories.R
\name{scan_trajectories}
\alias{scan_trajectories}
\title{Scan trajectories for diagnostic findings}
\usage{
scan_trajectories(
  x,
  scan_id = "scan-000001",
  repeat_threshold = 2L,
  loop_threshold = 3L,
  scans = NULL
)
}
\arguments{
\item{x}{A \link{TrajectoryBundle}.}

\item{scan_id}{A non-empty identifier for this diagnostic run.}

\item{repeat_threshold}{The minimum number of calls with the same tool name
and arguments that produces a \code{repeated_tool_call} finding.}

\item{loop_threshold}{The minimum consecutive calls with the same tool name
and arguments that produces a \code{suspicious_tool_loop} finding. Tool results
between calls do not break a call sequence.}

\item{scans}{Which detectors to run, as a character vector of names from
\code{\link[=scan_registry]{scan_registry()}}. \code{NULL} (the default) runs all of them. Selecting a
subset narrows the findings without changing how any of them are
computed, so a finding is the same whether or not its neighbours ran.}
}
\value{
A tibble with one row per finding and these columns:
\itemize{
\item \code{finding_id}: deterministic identifier within \code{scan_id}; \code{scan_id}: the
caller-supplied diagnostic-run identifier.
\item \code{scan}: one of \code{ambiguous_tool_correlation}, \code{unresolved_tool_call},
\code{unmatched_tool_result}, \code{repeated_tool_call}, \code{suspicious_tool_loop},
\code{event_error}, \code{error_chain}, \code{turn_error}, or \code{trajectory_error};
\code{scan_version}: detector version.
\item \code{trajectory_id}, \code{turn_id}, and \code{event_id}: primary evidence identities;
\code{event_ids}: character vector of every supporting event identifier. A
\code{trajectory_error} finding has no turn or event, and a \code{turn_error}
finding no event, because the failure is recorded on the record itself.
\item \code{severity}: \code{warning} for tool-pattern findings or \code{error} for failures;
\code{label} and \code{explanation}: concise human-readable descriptions.
\item \code{value}: scan-specific structured details; \code{metadata}: a named extension
list, currently empty for built-in scans.
}

No findings returns a typed zero-row tibble with the same columns.
}
\description{
\code{scan_trajectories()} analyzes a completed \link{TrajectoryBundle} without calling
a model or tool. It detects ambiguous, unresolved, and unmatched tool
correlation; repeated calls and suspicious consecutive loops; failed events;
and causal error chains. Named tool-argument mappings are compared without
regard to key order.
}
\details{
Finding identifiers are deterministic for an unchanged bundle, argument set,
and \code{scan_id}. The default \code{scan_id} is snapshot-local and is not intended as
a globally unique persistent identifier.
}
\examples{
bundle <- TrajectoryBundle(
  data.frame(trajectory_id = "trajectory-1", source_type = "manual"),
  data.frame(),
  data.frame()
)

scan_trajectories(bundle)
}
