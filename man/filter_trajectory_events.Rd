% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-trajectory-events.R
\name{filter_trajectory_events}
\alias{filter_trajectory_events}
\title{Filter trajectory events}
\usage{
filter_trajectory_events(
  x,
  trajectory_id = NULL,
  role = NULL,
  event_type = NULL,
  content_type = NULL,
  tool = NULL,
  status = NULL,
  metadata = NULL
)
}
\arguments{
\item{x}{A \link{TrajectoryBundle}.}

\item{trajectory_id, role, event_type, content_type, tool, status}{Optional
character vectors. An event must match one value in every supplied filter.}

\item{metadata}{An optional uniquely named list of top-level event metadata
values to match with exact R equality.}
}
\value{
A tibble containing the selected canonical event rows and any extra
source columns, followed by these derived turn-context columns:
\itemize{
\item Identity and order: character \code{trajectory_id}, \code{event_id}, and optional
\code{turn_id} and \code{parent_event_id}; integer \code{event_index} and optional
\code{content_index}.
\item Event description: character \code{event_type}, optional \code{content_type}, \code{name},
\code{call_id}, and \code{text}; list-column \code{value} for structured payloads.
\item Outcome and extensions: optional character \code{status} and \code{error}, plus the
named-list \code{metadata} column.
\item \code{.turn_role}: character role of the containing turn.
\item \code{.turn_index}: integer position of the containing turn.
\item \code{.round_index}: optional integer round of the containing turn.
}

Rows follow trajectory order and then \code{event_index}. A valid filter with no
matches returns a typed zero-row tibble with the same columns.
}
\description{
\code{filter_trajectory_events()} selects canonical events using event fields,
containing-turn context, or exact top-level metadata values. Filters compose
with AND across arguments and OR within each character vector.
}
\details{
The returned event columns are not modified. Three derived columns are added
after \code{turn_id}: \code{.turn_role}, \code{.turn_index}, and \code{.round_index}. These names
are reserved so source columns are never overwritten; a source event table
that already uses one of them produces a
\code{scans_error_scan_column_collision} error.
}
\examples{
bundle <- TrajectoryBundle(
  data.frame(trajectory_id = "trajectory-1", source_type = "manual"),
  data.frame(
    trajectory_id = "trajectory-1",
    turn_id = "turn-1",
    turn_index = 1L,
    role = "assistant"
  ),
  data.frame(
    trajectory_id = "trajectory-1",
    event_id = "event-1",
    event_index = 1L,
    turn_id = "turn-1",
    event_type = "content",
    content_type = "text",
    text = "Done"
  )
)

filter_trajectory_events(bundle, role = "assistant")
}
