% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/investigation-format.R
\name{investigation_files}
\alias{investigation_files}
\alias{write_investigation}
\alias{read_investigation}
\title{Write or read a saved investigation}
\usage{
write_investigation(x, path, overwrite = FALSE)

read_investigation(path, max_bytes = 50 * 1024^2)
}
\arguments{
\item{x}{A saved investigation from \code{\link[=investigation_snapshot]{investigation_snapshot()}} or
\code{read_investigation()}.}

\item{path}{Path to a local JSON file.}

\item{overwrite}{Whether to replace an existing file. Defaults to \code{FALSE}.}

\item{max_bytes}{Largest file to read, in bytes. Defaults to 50 MiB.}
}
\value{
\code{write_investigation()} returns \code{x} invisibly.
\code{read_investigation()} returns a \code{scans_investigation} value.
}
\description{
\code{write_investigation()} writes a value created by
\code{\link[=investigation_snapshot]{investigation_snapshot()}}. \code{read_investigation()} validates and reopens it
without contacting its source or rerunning scanners. Saved scanner identities
can be reopened even when they are absent from the installed registry.
Pass the result to
\code{\link[=scans_app]{scans_app()}} or inspect its \code{bundle}, \code{analysis}, \code{settings}, and \code{manifest}.
}
\details{
Files use the versioned scans JSON format. They contain retained content,
not an anonymized report. SHA-256 identifiers detect changes to the evidence
or analysis; they do not establish who created the file. Unsupported formats,
invalid references, and edited identifiers are rejected.
}
\examples{
bundle <- TrajectoryBundle(
  data.frame(trajectory_id = "run-1", source_type = "manual",
             status = "completed"),
  data.frame(), data.frame()
)
saved <- investigation_snapshot(bundle)
path <- tempfile(fileext = ".scans.json")
write_investigation(saved, path)
reopened <- read_investigation(path)
reopened$manifest$snapshot_id
}
