% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scans-annotations.R
\name{scans_annotations}
\alias{scans_annotations}
\title{Store trajectory annotations}
\usage{
scans_annotations(
  path = NULL,
  labels = c("looks right", "wrong answer", "tool failure", "follow up")
)
}
\arguments{
\item{path}{Path to the log file. Each line is one JSON record. Defaults to
\code{annotations.jsonl} under \code{tools::R_user_dir("scans", "data")}. On Posit
Connect, point this at a directory every viewer's process can write to;
the per-user default is not shared.}

\item{labels}{The labels a reviewer may apply, as a character vector.}
}
\value{
A \code{scans_annotations} object: a list of \code{read()}, \code{append()}, and
\code{labels} for the app to use.
}
\description{
\code{scans_annotations()} creates the annotation store the app writes to when a
reviewer labels or comments on a trajectory. Pass it to \code{\link[=scans_app]{scans_app()}} or
\code{\link[=scans_app_connect]{scans_app_connect()}} through their \code{annotations} argument; without one the
app stays strictly read-only and shows no annotation controls.

Annotations are an append-only log: saving never rewrites or deletes an
earlier record, and a trajectory's annotations are every record written
against it, newest first. A log rather than a mutable field means two
reviewers working at once cannot overwrite each other, and that a
judgement made about a trajectory remains visible after it is revised.
}
\examples{
store <- scans_annotations(path = tempfile(fileext = ".jsonl"))
store$append("trajectory-1", label = "follow up", note = "Tool loop here.")
store$read("trajectory-1")
}
