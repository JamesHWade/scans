% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectory-bundle.R
\name{TrajectoryBundle}
\alias{TrajectoryBundle}
\title{A canonical agent trajectory bundle}
\usage{
TrajectoryBundle(
  trajectories,
  turns,
  events,
  evaluations = NULL,
  losses = NULL
)
}
\arguments{
\item{trajectories}{A data frame with one row per logical agent trajectory.
Non-empty inputs require \code{trajectory_id} and \code{source_type}.}

\item{turns}{A data frame with one row per semantic turn. Non-empty inputs
require \code{trajectory_id}, \code{turn_id}, \code{turn_index}, and \code{role}.}

\item{events}{A data frame with one row per content or execution event.
Non-empty inputs require \code{trajectory_id}, \code{event_id}, \code{event_index}, and
\code{event_type}.}

\item{evaluations}{An optional data frame with outcome judgments. Non-empty
inputs require \code{trajectory_id} and \code{evaluation_id}.}

\item{losses}{An optional data frame recording source data that was
unsupported, redacted, truncated, or externalized. Non-empty inputs
require \code{field}, \code{reason}, and \code{detail}.}
}
\value{
A \code{TrajectoryBundle} S7 object.
}
\description{
\code{TrajectoryBundle()} creates a validated, source-neutral snapshot of one or
more completed agent trajectories. Its properties are ordinary tibbles for
trajectories, turns, events, evaluations, and adapter losses.
}
\details{
Missing optional canonical columns are filled with typed missing values.
Empty \code{evaluations} and \code{losses} tables are created when those arguments are
\code{NULL}. Unknown extra columns are preserved.
}
\examples{
bundle <- TrajectoryBundle(
  data.frame(
    trajectory_id = "trajectory-1",
    source_type = "manual"
  ),
  data.frame(),
  data.frame()
)

trajectory_info(bundle)
}
\section{Additional properties}{

\describe{
\item{\code{@schema_version}}{The integer trajectory schema version. Version 1 is the
only currently supported value.}
}}

