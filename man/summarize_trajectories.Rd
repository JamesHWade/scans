% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize-trajectories.R
\name{summarize_trajectories}
\alias{summarize_trajectories}
\title{Summarize trajectories}
\usage{
summarize_trajectories(x)
}
\arguments{
\item{x}{A \link{TrajectoryBundle}.}
}
\value{
A tibble with one row per trajectory and these columns:
\itemize{
\item Identity: \code{trajectory_id}, \code{run_id}, \code{parent_trajectory_id}, \code{source_type},
and \code{status} retain the corresponding trajectory fields.
\item Structure: \code{trajectory_depth} counts parent trajectories from a root at
zero; \code{max_event_depth} counts the longest event-parent path;
\code{n_turns}, \code{n_rounds}, and \code{n_events} count semantic records.
\item Tool correlation: \code{n_tool_calls}, \code{n_tool_results},
\code{n_unresolved_tool_calls}, \code{n_unmatched_tool_results}, and
\code{n_ambiguous_tool_correlations} count tool activity and correlation gaps.
\item Failures and losses: \code{n_error_events}, \code{n_failed_turns}, and \code{n_losses}.
\item Usage: \code{input_tokens}, \code{output_tokens}, and \code{cached_input_tokens} sum known
token counts; \code{cost} sums known source costs without currency conversion;
\code{turn_duration} sums known turn durations in seconds; \code{elapsed} is wall
time in seconds from \code{started_at} through \code{completed_at}.
}

Depths and counts are integers. Usage, cost, and duration columns are
doubles.
}
\description{
\code{summarize_trajectories()} reduces each trajectory in a \link{TrajectoryBundle}
to one row of identity, structure, diagnostic, and usage measures. Counts are
zero when no matching records exist. Usage totals are \code{NA} when every source
value contributing to that total is unknown.
}
\examples{
bundle <- TrajectoryBundle(
  data.frame(trajectory_id = "trajectory-1", source_type = "manual"),
  data.frame(),
  data.frame()
)

summarize_trajectories(bundle)
}
