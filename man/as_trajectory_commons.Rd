% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/commons-adapter.R
\name{as_trajectory_commons}
\alias{as_trajectory_commons}
\title{Convert completed commons conversations}
\usage{
as_trajectory_commons(x, trajectory_id = NULL, metadata = list())
}
\arguments{
\item{x}{An already-read, named list returned by
\code{commons::trajectory_read()}.}

\item{trajectory_id}{A bundle-unique trajectory ID. When \code{NULL}, a stable ID
is derived from the conversation name. It must be \code{NULL} when \code{x} contains
multiple conversations.}

\item{metadata}{A uniquely named list of safe application metadata, applied
to every conversation. Sensitive fields are redacted and recorded as
losses. Names used by commons source metadata are reserved.}
}
\value{
A \link{TrajectoryBundle}.
}
\description{
\code{as_trajectory_commons()} converts the already-read result of
\code{commons::trajectory_read()} into one canonical \link{TrajectoryBundle}. Each
named conversation becomes one trajectory, and its model-visible turns are
delegated to \code{\link[=as_trajectory_ellmer]{as_trajectory_ellmer()}}. The adapter does not read files,
connect to Posit Connect, or parse trace records itself.
}
\details{
Conversation provenance and the outer source descriptor are retained as
sanitized metadata. Each exchange provenance record also becomes a
\code{"commons:provenance"} event attached to the final turn in that exchange.
Missing or malformed source facts are reported as adapter losses rather than
inferred.

Because \code{commons::trajectory_read()} returns an ordinary list without a
discriminating class, use this explicit adapter instead of
\code{\link[=as_trajectory]{as_trajectory()}}.
}
\examples{
if (rlang::is_installed("ellmer", version = "0.4.2")) {
  conversation <- list(
    ellmer::UserTurn(list(ellmer::ContentText("Hello"))),
    ellmer::AssistantTurn(list(ellmer::ContentText("Hi")))
  )
  attr(conversation, "last_active") <- as.POSIXct(
    "2026-08-23 12:00:00",
    tz = "UTC"
  )
  attr(conversation, "provenance") <- list(list(
    provenance_tag = "A",
    citation_decisions = list()
  ))

  source <- list(`conversation-001` = conversation)
  attr(source, "source") <- list(kind = "local", path = "/traces")
  as_trajectory_commons(source)
}
}
