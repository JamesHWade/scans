% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsprrr-adapter.R
\name{as_trajectory_dsprrr}
\alias{as_trajectory_dsprrr}
\title{Convert completed dsprrr traces}
\usage{
as_trajectory_dsprrr(
  x,
  trajectory_id = NULL,
  source_uri = NULL,
  metadata = list()
)
}
\arguments{
\item{x}{A dsprrr module, or a data frame returned by
\code{dsprrr::export_traces()}.}

\item{trajectory_id}{A bundle-unique trajectory ID. When \code{NULL}, a stable ID
is derived from the program artifact ID and trace order. It must be \code{NULL}
when \code{x} contains multiple traces.}

\item{source_uri}{An optional source locator. Credentials, query parameters,
and fragments are removed.}

\item{metadata}{A uniquely named list of safe application metadata, applied
to every trace. Sensitive fields are redacted and recorded as losses.}
}
\value{
A \link{TrajectoryBundle}.
}
\description{
\code{as_trajectory_dsprrr()} snapshots one or more rows returned by
\code{dsprrr::export_traces()}, or a dsprrr module through that public exporter,
into a canonical \link{TrajectoryBundle}. Each trace row becomes one trajectory.
Model-visible turns are delegated to \code{\link[=as_trajectory_ellmer]{as_trajectory_ellmer()}} when present.
}
\details{
The program artifact digest becomes the source identity and is retained as
an integrity assurance, not an authenticity or trust claim. A caller-supplied
\code{run_id} or \code{deputy_run_id} in \code{trace_context} may become the canonical
\code{run_id}; product identifiers such as \code{research_run_id} remain correlation
metadata. The adapter never manufactures Deputy or Tempest ownership.

\code{as_trajectory()} dispatches to this adapter for authenticated dsprrr module
objects when dsprrr is installed. Exported trace data frames use this
explicit adapter because they have no discriminating class.
}
\examples{
traces <- tibble::tibble(
  timestamp = as.POSIXct("2026-01-01", tz = "UTC"),
  latency_ms = 10,
  input_tokens = 2L,
  cached_input_tokens = 0L,
  output_tokens = 1L,
  total_tokens = 3L,
  cost = 0,
  model = "fixture-model",
  prompt_length = 5L,
  prompt = "Hello",
  response = "Hi",
  program_artifact_id = paste0("sha256:", strrep("a", 64L)),
  trace_context = list(list(run_id = "run-001"))
)
as_trajectory_dsprrr(traces)
}
