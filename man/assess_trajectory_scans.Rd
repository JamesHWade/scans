% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess-trajectory-scans.R
\name{assess_trajectory_scans}
\alias{assess_trajectory_scans}
\title{Assess built-in trajectory scans and their evidence coverage}
\usage{
assess_trajectory_scans(
  x,
  scan_id = "scan-000001",
  repeat_threshold = 2L,
  loop_threshold = 3L,
  scans = NULL
)
}
\arguments{
\item{x}{A \link{TrajectoryBundle}.}

\item{scan_id}{A non-empty identifier for this diagnostic run.}

\item{repeat_threshold}{The minimum number of calls with the same tool name
and arguments that produces a \code{repeated_tool_call} finding.}

\item{loop_threshold}{The minimum consecutive calls with the same tool name
and arguments that produces a \code{suspicious_tool_loop} finding. Tool results
between calls do not break a call sequence.}

\item{scans}{Which detectors to run, as a character vector of names from
\code{\link[=scan_registry]{scan_registry()}}. \code{NULL} (the default) runs all of them. Selecting a
subset narrows the findings without changing how any of them are
computed, so a finding is the same whether or not its neighbours ran.}
}
\value{
A list with two tibbles:
\itemize{
\item \code{findings}: the findings from assessable scanners, with the same columns
and identities as \code{\link[=scan_trajectories]{scan_trajectories()}}. Candidates from scanners with
insufficient evidence are excluded. The findings-only API still returns
those raw candidates.
\item \code{assessments}: one row per selected scanner and trajectory, with \code{scan_id},
\code{trajectory_id}, \code{scan}, \code{scan_version}, \code{status}, \code{required_evidence},
\code{reason}, and list columns \code{settings}, \code{finding_ids}, \code{limitations}, and
\code{loss_rows}. \code{loss_rows} indexes \code{\link[=trajectory_losses]{trajectory_losses()}} in the input bundle,
including unassigned losses that may affect any trajectory.
}

Status is one of \code{assessed_no_findings}, \code{assessed_with_findings},
\code{insufficient_evidence}, \code{not_applicable}, or \code{execution_failure}.
An assessment describes only the retained records, never complete capture
or overall success. A positive error observation remains assessable when
other records lack status; limitations explain that negative conclusions
are unavailable. Missing semantic records do not establish inapplicability.

Tool comparisons require recorded identities or arguments as appropriate.
Known losses in required fields prevent assessment. Empty argument lists
are valid; missing arguments are not. Recorded text conversations without
tool activity make tool scans inapplicable to that capture. Status scans
require explicit status or failure evidence. Recognized status labels are
\code{completed}, \code{failed}, \code{cancelled}, \code{interrupted}, \code{running}, and \code{pending};
other labels do not establish status coverage. Multiple failed events without
recorded parent links cannot establish the absence of an error chain.

Built-ins execute in tool, event, and record groups. A group failure affects
its selected scanners on that trajectory; other groups continue. Failure
reasons omit arbitrary condition messages. Finding identifiers are local to
the run and can change if a previously failed group later succeeds.
}
\description{
\code{assess_trajectory_scans()} runs the built-in diagnostics and explains whether
each selected scanner could assess each trajectory. It uses the same
detectors as \code{\link[=scan_trajectories]{scan_trajectories()}} and never calls a model or tool.
}
\examples{
bundle <- TrajectoryBundle(
  data.frame(trajectory_id = "span-1", source_type = "manual",
             status = "completed"),
  data.frame(), data.frame()
)
result <- assess_trajectory_scans(bundle)
result$assessments[c("scan", "status", "reason")]
}
